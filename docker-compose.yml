services:
  gravity-rift:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - '5175:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: 'true'
